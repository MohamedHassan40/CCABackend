# Copy to .env.test for local integration/module tests (gitignored).
# Start DB: npm run db:test:up
# Migrate:   npm run db:test:migrate

DATABASE_URL=postgresql://postgres:postgres@localhost:5433/cloud_org_test?schema=public
JWT_SECRET=test-jwt-secret-minimum-32-characters-long
NODE_ENV=test
